/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['0d6gifolc5gc3tzu.public.blob.vercel-storage.com'],
    },
    reactStrictMode: true,
    productionBrowserSourceMaps: true,
  }

export default nextConfig;
